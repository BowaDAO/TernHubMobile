import { StyleSheet, Text, View } from "react-native";
import { AuthFrame } from "../types/type";

const SigninFrame = () => {
  return (
    <View>
      <Text>SigninFrame</Text>
    </View>
  );
};

export default SigninFrame;

const styles = StyleSheet.create({});
