import { StyleSheet, Text, View } from "react-native";

const JobCard = () => {
  return (
    <View>
      <Text>JobCard</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default JobCard;
