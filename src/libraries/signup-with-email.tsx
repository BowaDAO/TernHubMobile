import { SigninFrame } from "../components";
import { icon } from "../../constants";

const SignupWithEmail = () => {
  return (
    <SigninFrame
      icon={icon.google}
      label="Sign up with Email"
      onPress={() => {}}
    />
  );
};

export default SignupWithEmail;
